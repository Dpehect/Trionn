# Layered Homepage — Selected Cases Performance Revision

- Layer 2 now starts directly with Selected Cases.
- HOBRO / capabilities deck / Projects / Agency / Contact header elements are removed from view.
- Scroll distance is compressed so projects follow each other without long dead gaps.
- Video was re-encoded to 1280px, 30fps, all-I-frame H.264 for faster and smoother seeking.
- Scroll rendering now uses one requestAnimationFrame loop instead of creating a GSAP tween on every scroll update.
- Layer 1 remains unchanged apart from removal of the global header.
