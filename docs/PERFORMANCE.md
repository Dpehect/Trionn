# Performance Budget

- Initial JS: < 260 KB gzip
- Route JS: < 180 KB gzip
- Hero model: < 1.2 MB compressed
- Images: < 350 KB each
- LCP: < 2.5 s
- INP: < 200 ms
- CLS: < 0.1

Use `npm run analyze` for bundle inspection.
