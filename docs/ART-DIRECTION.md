# Art Direction Contract
- One hero object unique to the brand
- DOM and WebGL driven by shared scroll progress
- Route transitions reuse project accent colors
- Media transitions use distortion/mask behavior
- Mobile receives a separate art-directed composition
- All effects degrade gracefully by device quality
