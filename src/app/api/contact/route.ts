import { NextResponse } from "next/server";export async function POST(req:Request){try{const body=await req.json();if(body.website)return NextResponse.json({ok:true});if(!body.name||!body.email||!body.brief)return NextResponse.json({error:"Missing fields"},{status:400});console.info("Project brief",{name:body.name,email:body.email,company:body.company,budget:body.budget,brief:body.brief});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Invalid request"},{status:400})}}