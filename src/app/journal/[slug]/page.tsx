import Image from "next/image";
import { notFound } from "next/navigation";
import { journalEntries } from "@/data/editorial";
export default async function JournalEntry({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const entry=journalEntries.find((item)=>item.slug===slug);if(!entry)notFound();return <article className="container-shell py-12"><header className="max-w-5xl"><p className="eyebrow">{entry.category} / {entry.date}</p><h1 className="display mt-6">{entry.title}.</h1><p className="mt-8 max-w-2xl text-xl leading-8 text-[var(--muted)]">{entry.excerpt}</p></header><div className="media-frame relative mt-14 aspect-[16/9]"><Image src={entry.image} alt={entry.title} fill priority/></div><div className="mx-auto grid max-w-3xl gap-8 py-16 text-lg leading-8"><p>Clothing is experienced in motion. The line seen on a hanger becomes a different object once distance, posture and light enter the composition.</p><p>Our process therefore begins with repeated fitting rather than decoration. We remove what does not affect the experience and retain details that make daily use clearer.</p><blockquote className="border-l-2 pl-8 text-3xl leading-tight">The garment should become more personal as its surface records use.</blockquote><p>This approach produces fewer pieces, but each one carries a more specific role. The result is not minimalism as an image. It is reduction as a working method.</p></div></article>}
