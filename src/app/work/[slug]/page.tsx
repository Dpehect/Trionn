import { notFound } from "next/navigation";import { work } from "@/lib/data";import type { Metadata } from "next";
export function generateStaticParams(){return work.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=work.find(x=>x.slug===slug);return item?{title:item.title,description:item.summary}:{}}
export default async function CaseStudy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=work.find(x=>x.slug===slug);if(!item)notFound();return <><section className="section pt-40"><div className="container"><span className="label">{item.label}</span><p className="eyebrow text-muted mt-5">{item.sector}</p><h1 className="display mt-9 max-w-[12ch]">{item.title}</h1></div></section><section className="pb-28"><div className="container grid md:grid-cols-2 gap-12 border-t border-line pt-10"><div><p className="eyebrow text-muted">Context</p><p className="body-lg mt-5">{item.summary}</p></div><div><p className="eyebrow text-muted">Result</p><p className="body-lg mt-5">{item.result}</p><ul className="mt-10">{item.metrics.map(m=><li className="border-t border-line py-4" key={m}>{m}</li>)}</ul></div></div></section></>};