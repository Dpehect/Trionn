"use client";import { useState } from "react";import { work,WorkLabel } from "@/lib/data";import { WorkCard } from "@/components/work-card";
const labels=["All","Verified","Anonymised","Representative"] as const;export default function WorkPage(){const [filter,setFilter]=useState<(typeof labels)[number]>("All");const items=filter==="All"?work:work.filter(w=>w.label===filter as WorkLabel);return <section className="section pt-40"><div className="container"><p className="eyebrow text-muted">Selected work</p><h1 className="display mt-10 max-w-[10ch]">Evidence over theatre.</h1><p className="body-lg mt-12 max-w-2xl">Verified means the relationship and stated result can be substantiated. Anonymised protects client identity. Representative demonstrates an approach, not a client outcome.</p><div className="flex flex-wrap gap-2 mt-12" aria-label="Filter case studies">{labels.map(l=><button key={l} onClick={()=>setFilter(l)} className={`label ${filter===l?"bg-ink text-paper":""}`}>{l}</button>)}</div><div className="mt-16">{items.map((w,i)=><WorkCard key={w.slug} item={w} index={i}/>)}</div></div></section>}