import type { Metadata } from "next";
import { PageHero } from "@/components/ui/page-hero";
import { siteConfig } from "@/lib/site";
export const metadata: Metadata = { title: "Accessibility", description: "Softbridge Solutions accessibility commitment." };
export default function AccessibilityPage(){return <main><PageHero eyebrow="Accessibility" title="Designed to include, not exclude." description="Our production target is WCAG 2.2 AA, supported by keyboard navigation, semantic structure, readable contrast and reduced-motion behaviour."/><section className="pb-32"><div className="container-shell max-w-4xl space-y-10 text-lg leading-relaxed"><div><h2 className="text-3xl font-semibold">Current status</h2><p className="mt-4 text-black/65">This pre-launch build includes a skip link, visible focus states, reduced-motion support, labelled forms and semantic page landmarks. A manual assistive-technology audit should be completed against final content before publication.</p></div><div><h2 className="text-3xl font-semibold">Feedback</h2><p className="mt-4 text-black/65">Encountered a barrier? Contact <a className="underline" href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a> with the page, device and issue. We aim to acknowledge accessibility reports within two business days.</p></div></div></section></main>}
