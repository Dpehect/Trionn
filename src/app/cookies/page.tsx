import type { Metadata } from "next";
const updated = "24 July 2026";
export const metadata: Metadata = { title: "Cookie Notice", description: "Softbridge Solutions cookie and local-storage notice." };
export default function CookiePage(){return <main className="pb-32 pt-40 md:pt-52"><article className="container-shell grid gap-12 lg:grid-cols-[.5fr_1fr]"><header><p className="eyebrow text-black/45">Legal</p><h1 className="mt-7 text-6xl font-semibold tracking-[-.06em] md:text-8xl">Cookie Notice</h1><p className="mt-5 text-sm text-black/45">Last updated {updated}</p></header><div className="max-w-3xl space-y-12 text-lg leading-8 text-black/65"><section><h2 className="text-3xl font-semibold tracking-[-.04em] text-black">Current implementation</h2><p className="mt-5">The site stores one local preference named <code className="rounded bg-black/5 px-2 py-1 text-sm">softbridge-cookie-consent</code>. It records whether the visitor selected essential storage only or allowed optional analytics. It does not identify the visitor.</p></section><section><h2 className="text-3xl font-semibold tracking-[-.04em] text-black">Essential storage</h2><p className="mt-5">Essential storage is used to remember the consent choice and prevent the banner from appearing on every page. It is not used for advertising or profiling.</p></section><section><h2 className="text-3xl font-semibold tracking-[-.04em] text-black">Optional analytics</h2><p className="mt-5">No analytics provider is bundled in this package. When one is added, it must load only after an “all” consent event and this notice must be updated with provider, purpose, retention and withdrawal details.</p></section><section><h2 className="text-3xl font-semibold tracking-[-.04em] text-black">Changing the choice</h2><p className="mt-5">Until a dedicated preferences panel is connected, visitors can remove the stored preference through their browser’s site-data controls and reload the page.</p></section></div></article></main>}
