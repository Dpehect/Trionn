import type { Metadata } from "next";import { notFound } from "next/navigation";import { insights,getInsight } from "@/content/insights";
export function generateStaticParams(){return insights.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=getInsight(slug);return item?{title:item.title,description:item.excerpt}:{} }
export default async function InsightPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=getInsight(slug);if(!item)notFound();return <main><article className="pb-32 pt-40 md:pt-52"><header className="container-shell"><div className="flex flex-wrap gap-4 text-xs text-black/45"><span>{item.category}</span><span>·</span><span>{item.date}</span><span>·</span><span>{item.readTime}</span></div><h1 className="mt-8 max-w-6xl text-[clamp(4rem,9vw,9rem)] font-semibold leading-[.85] tracking-[-.075em]">{item.title}</h1><p className="ml-auto mt-12 max-w-2xl text-xl leading-relaxed text-black/60">{item.excerpt}</p></header><div className="container-shell mt-24 grid gap-12 border-t hairline pt-16 lg:grid-cols-[.55fr_1fr]"><aside className="eyebrow text-black/40">Softbridge Field Notes</aside><div className="max-w-3xl">{item.sections.map(section=><section key={section.heading} className="mb-20"><h2 className="text-4xl font-semibold tracking-[-.05em] md:text-5xl">{section.heading}</h2>{section.body.map(p=><p key={p} className="mt-7 text-lg leading-8 text-black/65">{p}</p>)}</section>)}</div></div></article></main>}
