import type { MetadataRoute } from "next";import { siteConfig } from "@/lib/site";import { projects } from "@/content/projects";import { services } from "@/content/services";import { insights } from "@/content/insights";
export default function sitemap():MetadataRoute.Sitemap{const pages=["","/work","/services","/process","/about","/insights","/careers","/contact","/privacy","/cookies","/accessibility","/terms"];return [...pages.map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.7})),...projects.map(x=>({url:`${siteConfig.url}/work/${x.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.8})),...services.map(x=>({url:`${siteConfig.url}/services/${x.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.8})),...insights.map(x=>({url:`${siteConfig.url}/insights/${x.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.65}))]}
