"use client"; import { useEffect,useState } from "react"; import Link from "next/link"; import { Menu,X,ArrowUpRight } from "lucide-react"; import { AnimatePresence,motion } from "motion/react"; import { siteConfig } from "@/lib/site"; import { cn } from "@/lib/utils";
export function Header(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>32);fn();addEventListener("scroll",fn,{passive:true});return()=>removeEventListener("scroll",fn)},[]);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <><header className={cn("fixed inset-x-0 top-0 z-50 transition-all duration-500",scrolled?"py-3":"py-6")}><div className={cn("container-shell flex items-center justify-between rounded-full px-4 py-3 transition-all duration-500",scrolled&&"bg-[#f4f5ef]/90 shadow-[0_12px_40px_rgba(12,20,18,.1)] backdrop-blur-xl")}><Link href="/" className="focus-ring flex items-center gap-3 rounded-full"><span className="grid size-8 place-items-center rounded-full bg-foreground text-xs font-bold text-background">S</span><span className="text-sm font-semibold tracking-[-.03em]">Softbridge Solutions</span></Link><nav className="hidden items-center gap-7 lg:flex">{siteConfig.nav.map(x=><Link className="focus-ring rounded-sm text-xs font-semibold uppercase tracking-[.12em] text-black/60 transition hover:text-black" key={x.href} href={x.href}>{x.label}</Link>)}</nav><button aria-expanded={open} aria-label="Open menu" onClick={()=>setOpen(true)} className="focus-ring flex items-center gap-2 rounded-full border border-black/15 px-4 py-2 text-xs font-semibold uppercase tracking-[.12em]"><span className="hidden sm:inline">Menu</span><Menu size={16}/></button></div></header><AnimatePresence>{open&&<motion.div initial={{clipPath:"circle(0% at 93% 5%)"}} animate={{clipPath:"circle(150% at 93% 5%)"}} exit={{clipPath:"circle(0% at 93% 5%)"}} transition={{duration:.9,ease:[.22,1,.36,1]}} className="fixed inset-0 z-[80] bg-[#0c1412] text-[#f4f5ef]"><div className="container-shell flex h-full flex-col py-6"><div className="flex items-center justify-between"><span className="eyebrow">Softbridge / Finland</span><button className="focus-ring rounded-full border border-white/20 p-3" onClick={()=>setOpen(false)} aria-label="Close menu"><X/></button></div><div className="my-auto grid gap-3">{siteConfig.nav.map((x,i)=><motion.div key={x.href} initial={{y:60,opacity:0}} animate={{y:0,opacity:1}} transition={{delay:.18+i*.08}}><Link onClick={()=>setOpen(false)} href={x.href} className="group flex items-center justify-between border-b border-white/15 py-3 text-[clamp(2.8rem,8vw,7rem)] font-semibold leading-none tracking-[-.06em]"><span>{x.label}</span><ArrowUpRight className="opacity-30 transition group-hover:rotate-45 group-hover:opacity-100"/></Link></motion.div>)}</div><div className="flex justify-between text-xs text-white/50"><span>Helsinki · Finland</span><span>EN / FI</span></div></div></motion.div>}</AnimatePresence></>}