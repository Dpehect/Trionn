import * as React from "react"; import { Slot } from "@radix-ui/react-slot"; import { cva, type VariantProps } from "class-variance-authority"; import { cn } from "@/lib/utils";
const variants=cva("focus-ring inline-flex items-center justify-center  text-sm font-semibold transition-[transform,background,color] duration-300 active:scale-[.98]",{variants:{variant:{primary:"bg-foreground text-background hover:bg-baltic",outline:"border border-current bg-transparent hover:bg-foreground hover:text-background",ghost:"hover:bg-black/5"},size:{sm:"h-10 px-4",md:"h-12 px-6",lg:"h-14 px-8"}},defaultVariants:{variant:"primary",size:"md"}});
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>,VariantProps<typeof variants>{asChild?:boolean} export function Button({className,variant,size,asChild=false,...props}:ButtonProps){const Comp=asChild?Slot:"button";return <Comp className={cn(variants({variant,size,className}))}{...props}/>}