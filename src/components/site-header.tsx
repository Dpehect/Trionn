"use client";
import Link from "next/link";import { useState } from "react";import { AnimatePresence,motion } from "framer-motion";import { Menu,X } from "lucide-react";
const nav=[["Services","/services/digital-products"],["Work","/work"],["Approach","/approach"],["Careers","/careers"],["Contact","/contact"]];
export function SiteHeader(){const [open,setOpen]=useState(false);return <><header className="fixed top-0 z-50 w-full mix-blend-difference text-white"><div className="container flex h-20 items-center justify-between"><Link href="/" className="focus-ring text-xl font-medium tracking-[-.04em]">Softbridge</Link><nav className="hidden md:flex gap-7 text-sm">{nav.map(([n,h])=><Link className="focus-ring" key={h} href={h}>{n}</Link>)}</nav><button aria-label="Open menu" className="focus-ring md:hidden" onClick={()=>setOpen(true)}><Menu/></button></div></header><AnimatePresence>{open&&<motion.div initial={{clipPath:"inset(0 0 100% 0)"}} animate={{clipPath:"inset(0 0 0% 0)"}} exit={{clipPath:"inset(0 0 100% 0)"}} transition={{duration:.7,ease:[.22,1,.36,1]}} className="fixed inset-0 z-[80] bg-ink text-paper"><div className="container flex h-20 justify-between items-center"><span className="text-xl">Softbridge</span><button aria-label="Close menu" onClick={()=>setOpen(false)}><X/></button></div><nav className="container pt-16">{nav.map(([n,h],i)=><motion.div key={h} initial={{y:50,opacity:0}} animate={{y:0,opacity:1}} transition={{delay:.15+i*.06}} className="border-t border-white/20 py-4"><Link onClick={()=>setOpen(false)} href={h} className="text-[13vw] leading-none tracking-[-.06em]">{n}</Link></motion.div>)}</nav></motion.div>}</AnimatePresence></>};