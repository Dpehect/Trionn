import Link from "next/link";import { WorkLabel } from "@/lib/data";export function WorkCard({item,index}:{item:{slug:string;label:WorkLabel;sector:string;title:string;summary:string;result:string};index:number}){return <Link href={`/work/${item.slug}`} className="group block border-t border-line py-7 md:py-10"><div className="grid md:grid-cols-[80px_1fr_1fr_24px] gap-4 md:gap-8 items-start"><span className="eyebrow text-muted">0{index+1}</span><div><span className="label">{item.label}</span><p className="eyebrow text-muted mt-3">{item.sector}</p></div><div><h3 className="title max-w-[16ch] group-hover:translate-x-2 transition-transform duration-500">{item.title}</h3><p className="mt-4 max-w-xl text-muted">{item.result}</p></div><span className="text-xl transition-transform group-hover:translate-x-1 group-hover:-translate-y-1">↗</span></div></Link>}